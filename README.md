# Deep-learning

This deep learning project aims to classify videos based on 51 distinct actions. To achieve this, we utilized a neural network architecture, specifically combining Convolutional Neural Networks (CNNs) with Long Short-Term Memory (LSTM) networks."
